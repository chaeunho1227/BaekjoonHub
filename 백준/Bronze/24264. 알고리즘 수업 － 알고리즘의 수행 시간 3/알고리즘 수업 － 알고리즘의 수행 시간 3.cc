#include <iostream>

using namespace std;

int main(){
    long long input, count , degree = 2;
    cin >> input;
    count = input * input;
    cout << count << endl << degree;
    return 0;
}
